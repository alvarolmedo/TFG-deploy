filebeat:
  prospectors:
    - input_type: log
      paths:
        - "/var/log/nginx/*.log"
      document_type: nginx
      encoding: posix
      ignore_older: 3h
############################# Output ##########################################
output:
  logstash:
    hosts: ["172.16.0.1:5044"]
