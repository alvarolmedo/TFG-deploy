filebeat:
  prospectors:
    - input_type: log
      paths:
        - "/var/log/nginx/*.log"
      encoding: utf-8
      ignore_older: 3h
############################# Output ##########################################
output:
  logstash:
    hosts: ["172.16.0.1:5044"]
